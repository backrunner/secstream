{
  "name": "secstream-demo",
  "version": "1.0.0",
  "description": "SecStream Demo Server using Hono",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && tsx watch --clear-screen=false server.ts",
    "start": "npm run build && tsx server.ts"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@hono/node-server": "^1.8.0",
    "tsx": "^4.7.0",
    "secstream": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@8.15.0"
}